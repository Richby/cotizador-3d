generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Filament {
  id          Int     @id @default(autoincrement())
  name        String  @unique
  costPerCubicCm Float
  density     Float

  @@map("filaments")
}